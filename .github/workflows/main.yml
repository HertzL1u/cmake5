name: Cmake-dll
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
jobs :
  build:
    name: MYJOB
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: CMAKE & PRINT
        run: |
          git clone https://github.com/HertzL1u/cmake5.git
          git add my_folder
          git commit -m "Add my_folder"
          git push
        shell: pwsh
